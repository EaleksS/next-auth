/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"i.imgur.com",
			"i.pinimg.com",
			"avatars.githubusercontent.com",
			"cdn.discordapp.com",
		],
	},
};

module.exports = nextConfig;
